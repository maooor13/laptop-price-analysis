In order to run the code you should create a new virtual environment (venv)  
Then source it, and run bash```pip install -r requirements.txt```  
After that you should just run bash```python main.py```.  
There would be text output and graphs that show up.
